# Copy this file to .streamlit/secrets.toml and fill in real values.
# secrets.toml itself is gitignored — never commit it with real credentials.

GROQ_API_KEY = "your-groq-api-key-here"

# Login accounts for the app's own sign-in screen — add as many as you like.
[allowed_users]
demo = "changeme"
